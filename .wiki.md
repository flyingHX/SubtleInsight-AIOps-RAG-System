# Project Summary
项目提供面向企业知识运营、智能运维与知识治理的统一控制台，帮助企业维护知识库、案例库、规则和 Agent 流程，并通过审批、权限、审计与配置能力保障知识资产质量和业务数据安全。

当前支持知识与案例管理、相似案例去重合并、审批流转、Agent 诊断与治理、告警事件反馈、规则管理、RBAC 权限控制、审计配置及帮助中心等功能。告警工作台支持桌面端常驻详情面板和 AI 诊断，规则管理支持无编辑权限用户只读查看规则 YAML、版本历史及未知告警队列。

# Project Module Description
- **知识库**：浏览和管理知识条目、案例及关联实例日志，支持标签筛选、详情、分页和变更记录。
- **去重合并**：扫描相似案例并按组管理候选，支持整组取消、恢复、确认弹窗及审计记录。
- **案例库**：展示完整案例字段、自动编号、关联日志统计和日志详情。
- **审批中心**：管理合并、规则升级、新建案例和案例更新审批，展示完整内容、证据及字段 Diff。
- **Agent 工作台**：提供深度诊断、知识治理、会话结果恢复及历史值班报告。
- **事件与告警反馈**：
  - 告警行选中后，桌面端直接在右侧常驻面板展示详情，不再弹出重复详情弹窗。
  - 移动端保留 Sheet 抽屉展示详情。
  - AI 诊断支持请求中、成功和失败状态反馈，并回填诊断结论、置信度、召回链路及原始 JSON。
  - 反馈操作按权限控制。
- **规则管理**：
  - 所有角色可查看规则 YAML、版本历史和未知告警队列。
  - 无 `can_edit_kb` 权限用户仅可读查看，编辑、校验、发布、回滚、晋升和废弃操作隐藏或由后端拒绝。
  - 具备编辑权限的用户可执行规则维护和未知告警处理。
- **用户与角色**：管理用户、角色和访问范围，受页面级权限保护。
- **审计与配置**：系统管理员专属，包含审计日志、系统配置和 LLM 连通性测试。
- **帮助中心**：提供使用手册和 FAQ，不包含运维部署入口。
- **权限门控**：前端隐藏无权限导航、页面和按钮，后端执行最终认证与授权校验。
- **错误与空数据处理**：安全处理权限不足、空队列、空分页结果、缺失字段和异步请求状态。

# Directory Tree
```text
app/
├── backend/
│   ├── routers/
│   │   ├── console.py                 # 知识库、案例、审批、规则、审计与配置路由
│   │   └── console_agent.py           # Agent、会话及值班报告路由
│   └── services/
│       ├── console_agent.py           # Agent 执行、结果持久化及报告序列化
│       └── console_kb.py              # 知识库、案例、日志及审批服务
└── frontend/
    └── src/
        ├── lib/
        │   └── console-api.ts         # 控制台 API 类型与调用封装
        ├── components/
        │   ├── shared.tsx             # 通用展示组件
        │   └── console/
        │       └── ConsoleLayout.tsx  # 布局、导航及权限入口
        └── pages/
            ├── HelpPage.tsx           # 使用手册与 FAQ
            └── console/
                ├── AgentsPage.tsx     # Agent 工作台与历史报告
                ├── ApprovalsPage.tsx  # 审批列表、详情及操作
                ├── EventsPage.tsx     # 告警详情、AI 诊断与反馈
                ├── KbPage.tsx         # 知识库、案例、日志及去重合并
                ├── OpsPage.tsx        # 审计日志与系统配置
                └── RulesPage.tsx      # 规则 YAML、版本及未知队列管理
verify_approval_content_tmp.py         # 审批内容验收脚本
verify_round3_tmp.py                   # 知识库和 Agent API 回归脚本
```

# File Description Inventory
| 文件 | 说明 |
|---|---|
| `app/backend/routers/console.py` | 提供知识库、案例、日志、审批、规则、审计和配置接口，并执行相关权限校验。 |
| `app/backend/routers/console_agent.py` | 提供 Agent 时间范围、会话结果及历史值班报告接口。 |
| `app/backend/services/console_agent.py` | 执行并持久化诊断、治理会话结果，恢复最近结果并序列化报告。 |
| `app/backend/services/console_kb.py` | 处理案例日志统计、案例详情、去重合并、规则和审批内容。 |
| `app/frontend/src/components/console/ConsoleLayout.tsx` | 根据权限矩阵显示或隐藏控制台导航入口。 |
| `app/frontend/src/components/shared.tsx` | 提供卡片、弹窗、表格和状态展示等通用组件。 |
| `app/frontend/src/lib/console-api.ts` | 定义控制台 API 类型、分页模型、诊断请求和权限相关调用。 |
| `app/frontend/src/pages/HelpPage.tsx` | 提供使用手册和 FAQ。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示 Agent 诊断、知识治理、会话恢复和历史值班报告。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 展示审批详情、字段 Diff 及权限控制的审批操作。 |
| `app/frontend/src/pages/console/EventsPage.tsx` | 管理告警列表、桌面端右侧详情面板、移动端详情抽屉、AI 诊断和反馈操作。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 管理案例、日志和去重合并。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 提供审计日志和系统配置，并执行管理员页面守卫。 |
| `app/frontend/src/pages/console/RulesPage.tsx` | 展示规则 YAML、版本历史和未知告警队列，并按权限控制写操作。 |
| `verify_approval_content_tmp.py` | 验证审批内容、权限和空数据处理。 |
| `verify_round3_tmp.py` | 回归验证案例日志、去重合并、Agent、报告及认证权限。 |

# Technology Stack
- **前端**：React、TypeScript、Vite
- **后端**：Python、FastAPI 风格路由及服务层
- **数据请求**：控制台 API 封装、分页响应、诊断请求、会话和报告序列化
- **权限模型**：前后端 RBAC、页面级和操作级权限门控
- **数据能力**：案例字段、关联实例日志、规则 YAML、版本历史、字段 Diff 和审计记录
- **Agent 能力**：时间窗口治理、诊断会话持久化、结果恢复和值班报告
- **构建与检查**：npm、ESLint、Vite 生产构建、API 回归和浏览器验收

# Usage
## 安装前端依赖
```bash
cd app/frontend
npm install
```

## 代码检查
```bash
npm run lint
```

## 生产构建
```bash
npm run build
```

## 启动开发环境
```bash
npm run dev
```

## 审批内容验收
在后端服务和测试数据库准备完成后运行：
```bash
python verify_approval_content_tmp.py
```

## API 回归
在后端服务和测试数据库准备完成后运行：
```bash
python verify_round3_tmp.py
```

回归重点：
- 案例关联日志、去重合并和审计行为。
- Agent 诊断、时间范围、会话恢复和值班报告。
- 告警桌面端详情面板、AI 诊断请求及结果回填。
- 规则 YAML 和未知告警队列的只读权限。
- 普通角色与管理员的接口权限差异。
- 前端 lint、生产构建及浏览器端到端表现。
