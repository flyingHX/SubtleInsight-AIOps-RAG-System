# Project Summary
项目提供面向企业知识运营、智能运维与知识治理的统一控制台，帮助企业维护知识库、案例库、规则和 Agent 流程，并通过审批、去重合并、权限管理、审计日志与系统配置保障知识资产质量和业务数据安全。

当前系统支持：
- 知识库案例、关联实例日志、审批内容及变更记录管理。
- 相似案例扫描、按案例组取消或恢复合并候选。
- 合并、规则升级、新建案例和案例更新审批。
- Agent 深度诊断、知识治理及历史值班报告。
- 告警事件反馈、规则管理和运维操作。
- 基于 RBAC 的导航、页面、操作按钮及后端接口权限控制。
- 审计与配置功能仅限系统管理员访问。
- 使用手册和 FAQ 帮助内容。

# Project Module Description
- **知识库**：浏览和管理知识条目、案例及关联实例日志，支持标签筛选、案例详情、分页和变更记录。
  - 案例卡片显示关联实例日志数量。
  - 案例详情和新建案例页面支持日志展示或预览。
- **去重合并**：扫描相似案例并按相似案例组管理合并候选。
  - 支持对整组结果执行“取消本次合并”。
  - 取消前使用应用内确认弹窗。
  - 取消后将整组移入已取消区域，合并计数归零。
  - 支持恢复整组结果并返回扫描结果区域。
  - 取消与恢复操作受 `can_edit_kb` 控制并记录审计信息。
- **案例库**：展示完整案例字段、自动编号、关联日志统计及日志详情。
- **审批中心**：按“待我审批”“我发起的”“我已处理”查看审批事项。
  - 合并审批展示合并策略、原因及双方完整案例内容。
  - 规则升级审批展示未知模板、升级规则条目及关联日志证据。
  - 新建案例审批展示完整字段、缺失字段和关联日志。
  - 案例更新审批展示变更前后快照及字段级 Diff。
  - 审批和撤回按钮按权限矩阵控制。
- **Agent 工作台**：
  - 深度诊断支持会话结果恢复。
  - 知识治理支持 `1h`、`24h`、`7d` 时间范围及最近结果恢复。
  - 值班报告支持历史列表和详情展开。
  - 诊断、治理等操作按权限隐藏或禁用。
- **事件与告警反馈**：查看告警事件并提交反馈，反馈操作按权限控制。
- **规则管理**：查看规则及未知队列，编辑和发布操作按权限控制。
- **用户与角色**：管理用户、角色和访问范围，用户管理页面受页面级权限保护。
- **审计与配置**：系统管理员专属模块。
  - 审计日志查询、配置查询与更新、LLM 连通性测试均由后端强制校验系统管理员角色。
  - 无权限用户不显示导航入口，直接访问页面或接口会被拒绝。
- **帮助中心**：保留使用手册和 FAQ，移除运维部署说明及相关入口。
- **权限门控**：前端隐藏无权限导航、页面和操作按钮，后端通过认证依赖执行最终校验。
- **错误与空数据处理**：安全处理权限不足、无匹配日志、空分页结果及缺失字段。

# Directory Tree
```text
app/
├── backend/
│   ├── routers/
│   │   ├── console.py                 # 知识库、案例、审批、审计与配置路由
│   │   └── console_agent.py           # Agent、会话及值班报告路由
│   └── services/
│       ├── console_agent.py           # Agent 执行、结果持久化及报告序列化
│       └── console_kb.py              # 知识库、案例、日志及审批内容服务
└── frontend/
    └── src/
        ├── lib/
        │   └── console-api.ts         # 控制台 API 类型与调用封装
        ├── components/
        │   ├── shared.tsx             # 控制台通用展示组件
        │   └── console/
        │       └── ConsoleLayout.tsx  # 控制台布局、导航及权限入口
        └── pages/
            ├── HelpPage.tsx           # 使用手册与 FAQ 页面
            └── console/
                ├── AgentsPage.tsx     # Agent 工作台、会话和历史报告
                ├── ApprovalsPage.tsx  # 审批列表、详情及操作
                ├── EventsPage.tsx     # 事件与告警反馈
                ├── KbPage.tsx         # 知识库、案例、日志及去重合并
                ├── OpsPage.tsx        # 审计日志与系统配置
                └── RulesPage.tsx      # 规则及未知队列管理
verify_approval_content_tmp.py         # 审批内容接口验收脚本
verify_round3_tmp.py                   # 知识库和 Agent API 回归脚本
```

# File Description Inventory
| 文件 | 说明 |
|---|---|
| `app/backend/routers/console.py` | 提供知识库、案例、关联日志、审批、审计和配置接口，并对审计与配置接口执行系统管理员校验。 |
| `app/backend/routers/console_agent.py` | 提供 Agent 时间范围、会话结果及历史值班报告接口。 |
| `app/backend/services/console_agent.py` | 执行并持久化诊断、治理会话结果，恢复最近结果并序列化值班报告。 |
| `app/backend/services/console_kb.py` | 处理案例日志统计、日志序列化、案例详情、去重合并及审批内容。 |
| `app/frontend/src/components/console/ConsoleLayout.tsx` | 根据权限矩阵显示或隐藏控制台导航入口。 |
| `app/frontend/src/components/shared.tsx` | 提供通用卡片、弹窗、表格和状态展示组件。 |
| `app/frontend/src/lib/console-api.ts` | 定义控制台 API 类型、分页模型、权限相关请求及调用方法。 |
| `app/frontend/src/pages/HelpPage.tsx` | 提供使用手册和 FAQ，移除运维部署入口。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示 Agent 诊断、知识治理、会话恢复和历史值班报告，并控制操作权限。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 展示审批详情、字段 Diff 及按权限控制的审批操作。 |
| `app/frontend/src/pages/console/EventsPage.tsx` | 展示事件和告警反馈，并隐藏无权限反馈操作。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 管理案例、日志和去重合并，支持整组合并取消、恢复及应用内确认弹窗。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 提供审计日志和系统配置页面，并执行系统管理员页面守卫。 |
| `app/frontend/src/pages/console/RulesPage.tsx` | 管理规则和未知队列，按权限控制编辑与发布入口。 |
| `verify_approval_content_tmp.py` | 验证合并、新建、升级及更新审批内容、权限和空数据处理。 |
| `verify_round3_tmp.py` | 回归验证案例日志、去重合并、Agent 会话、值班报告及认证权限。 |

# Technology Stack
- **前端**：React、TypeScript、Vite
- **后端**：Python、FastAPI 风格路由、知识库及 Agent 服务
- **数据请求**：控制台 API 封装、分页响应、会话和报告序列化
- **权限模型**：前后端 RBAC、系统管理员接口校验、页面与操作级门控
- **数据能力**：案例完整字段、关联实例日志、规则预览、字段级 Diff、审计记录
- **Agent 能力**：时间窗口治理、诊断会话持久化、结果恢复、值班报告详情
- **构建与检查**：npm、ESLint、Vite 生产构建、API 回归及浏览器验收

# Usage
## 安装前端依赖
```bash
cd app/frontend
npm install
```

## 代码检查
```bash
npm run lint
```

## 生产构建
```bash
npm run build
```

## 启动开发环境
```bash
npm run dev
```

## 审批内容验收
在后端服务和测试数据库准备完成后运行：
```bash
python verify_approval_content_tmp.py
```

## API 回归
在后端服务和测试数据库准备完成后运行：
```bash
python verify_round3_tmp.py
```

回归重点：
- 案例关联日志数量、详情和预览。
- 去重合并整组取消、恢复及审计行为。
- Agent 时间范围和最近会话结果恢复。
- 深度诊断及历史值班报告详情。
- 系统管理员与普通角色的接口权限差异。
- 无权限导航、页面和操作按钮隐藏。
- 前端 lint 和生产构建。
