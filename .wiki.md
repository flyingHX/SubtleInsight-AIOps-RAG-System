# Project Summary
项目提供面向企业知识运营与系统管理的控制台，支持知识库、案例库、变更记录、知识去重合并、用户角色管理、审计日志及系统配置。通过前后端 RBAC 控制不同角色的操作范围，帮助企业维护高质量知识资产并保障运维配置与业务数据安全。

# Project Module Description
- **知识库**：浏览和管理知识条目，支持按标签查看内容。
- **去重合并**：扫描相似案例、查看合并提案并执行知识合并，避免重复内容。
- **案例库**：展示和管理案例记录。
- **变更记录**：查看知识库变更单及操作历史。
- **用户与角色**：管理用户、角色及访问入口，仅授权角色可用。
- **审计日志**：供运维人员查看系统操作记录。
- **配置中心**：仅系统管理员及以上角色可访问和修改 LLM、Embedding、诊断审批策略及系统配置。
- **权限门控**：前端按角色隐藏无权标签和菜单并阻止无权限请求，后端执行最终权限校验。
- **错误提示**：权限不足时展示友好提示，避免直接暴露 403 错误或空白页面。
- **分页数据处理**：知识库接口按 `{items: [...]}` 分页结构解析，空数据时安全降级为空列表。

# Directory Tree
```text
app/
└── frontend/
    └── src/
        ├── lib/
        │   └── console-api.ts       # 控制台及知识库 API 类型与调用封装
        └── pages/
            └── console/
                ├── KbPage.tsx       # 知识库、案例库、变更记录及去重合并页面
                └── OpsPage.tsx      # 审计与配置页面，按角色控制标签展示和配置请求
```

# File Description Inventory
| 文件 | 说明 |
|---|---|
| `app/frontend/src/lib/console-api.ts` | 定义控制台 API 调用及数据类型；将合并提案列表对齐为分页响应结构 `Paged<MergeProposal>`。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 知识库页面；读取分页响应中的 `items`，展示去重合并提案、相似案例、案例库和变更记录，并处理空数据。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 运维控制台页面；为配置中心提供角色门控，非管理员不发起配置请求并隐藏无权限入口。 |

# Technology Stack
- 前端：React、TypeScript
- 数据请求：控制台 API 封装、分页响应模型
- 构建与检查：npm、Lint、生产构建
- 权限模型：前后端 RBAC
- 验证范围：知识库标签页、去重合并与相似案例扫描、API 数据解析、权限提示、前端构建与回归测试

# Usage
## 安装依赖
```bash
cd app/frontend
npm install
```

## 代码检查
```bash
npm run lint
```

## 生产构建
```bash
npm run build
```

## 启动开发环境
```bash
npm run dev
```

## 功能验证
- 进入知识库页面，确认“去重合并”标签正常渲染且提案记录完整显示。
- 点击“扫描相似案例”，确认聚类结果正常展示。
- 验证“案例库”和“变更记录”标签可正常加载。
- 使用非编辑角色验证权限提示和只读行为。
- 使用非管理员账号进入运维页面，确认仅显示有权限内容且不发起配置请求。
- 使用系统管理员账号确认配置中心及配置数据正常加载。
- 执行前端 lint 与生产构建，确认代码可正常编译。
