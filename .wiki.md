# Project Summary

项目提供面向企业知识运营、智能运维与知识治理的统一 AIOps 平台，包含 RAG 告警诊断流水线和 Web 控制台。平台接收外部告警，经标准化、消息队列、缓存、规则与向量检索完成根因诊断，并通过控制台维护知识库、案例、规则、Agent 流程及审批治理。

当前支持告警接入与诊断、案例反馈闭环、知识库管理、相似案例去重合并、审批流转、规则治理、Agent 诊断、审计配置及 RBAC 权限控制。RAG 流水线包含分阶段延迟监控、Embedding 缓存与熔断、Milvus 查询缓存、LLM 超时降级及诊断接口非阻塞化。

项目已补齐开源发布所需的许可证、贡献指南、行为准则、安全策略、环境变量模板和 CI 工作流，并强化敏感文件与构建产物的忽略规则。安装包、上传图片和临时脚本不应提交到 GitHub，安装包应作为 Release 附件发布。

# Project Module Description

- **RAG 告警流水线**：接收 Webhook，标准化告警、生成事件指纹、写入 Kafka/Redis，并通过 Milvus、规则和 LLM 完成诊断。
- **P99 性能治理**：
  - 使用 `rag_stage_latency_seconds` Histogram 监控 Embedding、Milvus 检索、重排和 LLM 阶段延迟。
  - Embedding 支持 LRU 缓存、连续失败熔断和本地模型后台预热。
  - Milvus 支持集合句柄 TTL 缓存、查询超时和可配置 `nprobe`。
  - LLM 统一控制超时、重试和熔断，超时后快速降级并返回相似案例。
  - 诊断接口通过线程池执行同步 RAG 逻辑，避免阻塞事件循环。
  - Kafka 消费路径兼容字典格式事件。
- **健康检查与监控**：提供 `/healthz`、`/readyz` 和 `/metrics`。
- **知识库**：管理知识条目、案例、版本、关联实例日志和变更记录。
- **去重合并**：扫描相似案例、管理合并提案、确认或撤销合并并记录审计。
- **审批中心**：处理案例变更、规则升级、新建和更新案例审批，展示证据及字段 Diff。
- **Agent 工作台**：支持深度诊断、知识治理、会话恢复、CMDB 查询和历史值班报告。
- **事件与告警反馈**：展示 AI 诊断状态、结论、置信度、召回链路、原始 JSON 和反馈操作。
- **规则管理**：支持规则查看、校验、发布、回滚、晋升、废弃及未知告警队列治理。
- **认证与权限**：支持 OIDC、Token Exchange、演示登录、登出和 Bearer JWT，后端执行最终授权。
- **用户与角色**：管理用户、角色及访问范围，角色层级为 `viewer < operator < sre < approver < kb_admin < sys_admin`。
- **审计与配置**：系统管理员维护审计日志、系统配置和 LLM 连通性。
- **帮助中心**：提供用户手册、FAQ、部署手册和值班应急手册。
- **开源发布治理**：
  - 使用 Apache-2.0 许可证。
  - 通过贡献指南、行为准则和安全策略规范社区协作。
  - 使用根目录及 RAG 子项目 `.gitignore` 排除密钥、日志、上传文件、临时脚本、缓存和安装包。
  - 后端、前端和 RAG 分别提供 `.env.example`，仅包含配置占位符。
  - GitHub Actions 执行 RAG 测试、后端静态检查及前端 lint/build。
  - 发布前按照开源检查清单执行跟踪文件清理和敏感信息复扫。

## 主要接口

- RAG：`POST /api/v1/webhook`、`POST /api/v1/diagnostic`、`POST /api/v1/feedback`、`POST /api/v1/cases/close`
- RAG 探针：`GET /healthz`、`GET /readyz`、`GET /metrics`
- 认证：`POST /api/v1/auth/token/exchange`、`GET /api/v1/auth/me`、`GET /api/v1/auth/login`、`GET /api/v1/auth/callback`、`GET /api/v1/auth/logout`、`POST /api/v1/auth/demo-login`
- 控制台：覆盖权限、总览、事件、案例、版本、变更集、关联事件、去重合并、审批、规则、未知模板、审计和配置。
- Agent：覆盖诊断、知识治理、值班报告、会话、CMDB 和历史报告。
- 实体 CRUD：提供平台生成实体的标准分页增删改查接口；业务集成优先使用带权限和审计的控制台语义接口。

# Directory Tree

```text
README.md                              # 项目总览、模块边界、开源与安全说明
INSTALL.md                             # 安装与部署说明
LICENSE                                # Apache-2.0 许可证
CONTRIBUTING.md                        # 贡献、测试与 PR 规范
CODE_OF_CONDUCT.md                     # 社区行为准则
SECURITY.md                            # 漏洞报告与安全部署要求
install.sh                             # 一键安装脚本
.github/
└── workflows/
    └── ci.yml                         # GitHub Actions 持续集成
docs/
├── DEMO_ACCOUNTS.md                   # 演示账号及中间件默认凭证
├── CONSOLE_USER_GUIDE.md              # 控制台使用手册
├── CONSOLE_FAQ.md                     # 控制台常见问题
├── OPERATIONS_DEPLOYMENT_GUIDE.md     # 中间件架构与部署方案
├── OPERATIONS_RUNBOOK.md              # 值班、应急与故障处理手册
└── OPEN_SOURCE_RELEASE_CHECKLIST.md   # 开源发布审计与检查清单
aiops-rag-system/
├── README.md                          # RAG 流水线、性能治理与测试说明
├── docker-compose.yml                 # RAG 依赖服务编排
├── .env.example                       # RAG 环境变量及性能参数模板
├── .gitignore                         # RAG 子项目忽略规则
├── src/
│   ├── main.py                         # FastAPI 应用、业务接口与健康探针
│   ├── config.py                       # 运行配置与环境变量解析
│   ├── kafka_producer.py               # Kafka 消息生产
│   ├── api/
│   │   └── diagnostic.py               # 诊断接口与非阻塞执行入口
│   ├── rag_pipeline/
│   │   ├── embedder.py                 # Embedding、缓存、熔断与预热
│   │   ├── llm_client.py               # LLM 调用、超时、重试与降级
│   │   ├── milvus_client.py            # Milvus 连接、缓存与检索
│   │   └── pipeline.py                 # RAG 主流程及分阶段耗时统计
│   ├── storage/
│   │   └── redis_client.py             # Redis 事件暂存与状态访问
│   └── utils/
│       └── metrics.py                  # Prometheus 指标定义与采集
└── tests/
    ├── test_api.py                     # RAG API 与探针测试
    └── test_p99_optimizations.py       # P99 优化专项回归测试
app/
├── backend/
│   ├── README.md                       # 控制台后端边界与运行说明
│   ├── .env.example                    # 后端环境变量模板
│   ├── routers/
│   │   ├── console.py                  # 控制台业务路由
│   │   └── console_agent.py            # Agent、会话和报告路由
│   └── services/
│       ├── console_agent.py             # Agent 执行与结果持久化
│       └── console_kb.py                # 知识库、案例、日志和审批服务
└── frontend/
    ├── README.md                       # 控制台前端边界与运行说明
    ├── .env.example                    # 前端环境变量模板
    ├── pnpm-lock.yaml                  # 前端依赖锁定文件
    ├── dist/                           # 本地或构建生成的生产产物
    └── src/
        ├── lib/console-api.ts          # API 类型与调用封装
        ├── components/
        │   ├── shared.tsx              # 通用 UI 组件
        │   └── console/ConsoleLayout.tsx # 布局、导航和权限入口
        └── pages/
            ├── HelpPage.tsx             # 帮助中心
            └── console/
                ├── AgentsPage.tsx       # Agent 工作台
                ├── ApprovalsPage.tsx    # 审批中心
                ├── EventsPage.tsx       # 告警、诊断与反馈
                ├── KbPage.tsx           # 知识库与去重合并
                ├── OpsPage.tsx          # 审计与配置
                └── RulesPage.tsx        # 规则与未知告警队列
```

安装包 `aiops-suite-*.tar.gz`、`uploads/` 图片、日志、临时脚本和其他构建产物不纳入源码目录或 Git 跟踪；安装包应通过 Release 附件交付。

# File Description Inventory

| 文件 | 说明 |
|---|---|
| `README.md` | 说明产品能力、RAG 流水线、控制台及开源发布要求。 |
| `INSTALL.md` | 说明环境准备、依赖安装、配置生成和部署步骤。 |
| `install.sh` | 执行环境检查、依赖安装、配置初始化和前端构建。 |
| `LICENSE` | 项目 Apache-2.0 许可证文本。 |
| `CONTRIBUTING.md` | 说明双子系统开发、测试、提交和 PR 规范。 |
| `CODE_OF_CONDUCT.md` | 规定社区参与和协作行为准则。 |
| `SECURITY.md` | 说明漏洞报告渠道及生产环境密钥配置要求。 |
| `.github/workflows/ci.yml` | 执行 RAG pytest、后端 compileall、前端 ESLint 和构建。 |
| `docs/OPEN_SOURCE_RELEASE_CHECKLIST.md` | 提供发布前文件审计、敏感信息复扫和 Release 检查命令。 |
| `app/backend/.env.example` | 提供数据库、JWT、`MASK_KEY` 和连接池等后端配置占位符。 |
| `app/frontend/.env.example` | 提供前端构建和 API 相关环境变量占位符。 |
| `app/frontend/pnpm-lock.yaml` | 锁定前端依赖版本，应提交到源码仓库。 |
| `aiops-rag-system/.gitignore` | 排除 RAG 子项目中的环境文件、缓存、日志和临时产物。 |
| `aiops-rag-system/docker-compose.yml` | 编排 RAG 依赖服务及开发环境默认配置。 |
| `aiops-rag-system/.env.example` | 提供 RAG 运行、超时、重试、熔断、缓存和检索参数示例。 |
| `aiops-rag-system/src/main.py` | 实现 Webhook、诊断、反馈、案例闭环、健康检查和指标接口。 |
| `aiops-rag-system/src/config.py` | 读取并校验 RAG 运行参数。 |
| `aiops-rag-system/src/api/diagnostic.py` | 通过线程池承载同步诊断逻辑，避免阻塞异步事件循环。 |
| `aiops-rag-system/src/kafka_producer.py` | 封装 Kafka 事件发布能力。 |
| `aiops-rag-system/src/rag_pipeline/embedder.py` | 实现 Embedding 调用、LRU 缓存、熔断和本地模型预热。 |
| `aiops-rag-system/src/rag_pipeline/llm_client.py` | 实现 LLM 超时、重试抑制、熔断和快速降级。 |
| `aiops-rag-system/src/rag_pipeline/milvus_client.py` | 管理 Milvus 连接、集合句柄缓存、查询超时和相似案例检索。 |
| `aiops-rag-system/src/rag_pipeline/pipeline.py` | 编排检索、重排和 LLM 诊断，并记录分阶段延迟。 |
| `aiops-rag-system/src/storage/redis_client.py` | 管理告警暂存、事件查询和依赖就绪检查。 |
| `aiops-rag-system/src/utils/metrics.py` | 定义分阶段延迟、熔断和降级 Prometheus 指标。 |
| `aiops-rag-system/tests/test_api.py` | 验证 RAG API、健康探针和依赖降级行为。 |
| `aiops-rag-system/tests/test_p99_optimizations.py` | 覆盖 P99 优化、超时、缓存、熔断和降级场景。 |
| `app/backend/routers/console.py` | 提供认证后的事件、知识库、审批、规则、审计和配置路由。 |
| `app/backend/routers/console_agent.py` | 提供 Agent 诊断、知识治理、会话、CMDB 和值班报告路由。 |
| `app/backend/services/console_agent.py` | 执行 Agent 任务、持久化会话结果并序列化报告。 |
| `app/backend/services/console_kb.py` | 处理案例日志、去重合并、规则和审批数据。 |
| `app/frontend/dist/` | 提供可由静态服务器托管的构建产物，不作为源码发布必需内容。 |
| `app/frontend/src/lib/console-api.ts` | 定义控制台 API 类型、分页、诊断和权限调用。 |
| `app/frontend/src/components/console/ConsoleLayout.tsx` | 根据权限矩阵显示控制台导航和入口。 |
| `app/frontend/src/components/shared.tsx` | 提供卡片、弹窗、表格和状态展示组件。 |
| `app/frontend/src/pages/HelpPage.tsx` | 展示项目手册和 FAQ 内容。 |
| `app/frontend/src/pages/console/AgentsPage.tsx` | 展示 Agent 任务、会话恢复和历史报告。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 展示审批详情、证据、字段 Diff 和决策操作。 |
| `app/frontend/src/pages/console/EventsPage.tsx` | 管理告警详情、AI 诊断和反馈。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 管理案例、关联日志和相似案例合并。 |
| `app/frontend/src/pages/console/OpsPage.tsx` | 提供管理员专属审计和配置页面。 |
| `app/frontend/src/pages/console/RulesPage.tsx` | 展示规则 YAML、版本历史和未知告警队列。 |

# Technology Stack

- **RAG 后端**：Python、FastAPI、Uvicorn
- **控制台后端**：Python、FastAPI 风格路由与服务层
- **控制台前端**：React、TypeScript、Vite
- **消息与存储**：Kafka、Redis、Milvus
- **智能能力**：规则引擎、向量检索、LLM、CMDB、Agent 会话持久化
- **性能与可观测性**：Prometheus Histogram、阶段延迟、熔断与降级指标
- **认证授权**：OIDC、JWT、RBAC、页面级和操作级权限控制
- **交付与部署**：Docker Compose、Bash 安装脚本、Nginx 静态托管、GitHub Release
- **质量保障**：GitHub Actions、pytest、Python compileall、ESLint、Vite、Bash 语法检查
- **开源治理**：Apache-2.0、Contributor Covenant、贡献指南、安全策略和发布检查清单

# Usage

## 安装依赖

使用安装包或源码目录执行：

```bash
bash install.sh
```

安装脚本支持分步安装：

```bash
bash install.sh --skip-rag
bash install.sh --skip-backend
bash install.sh --skip-frontend
```

脚本会检查运行环境，安装 RAG 与控制台后端的 Python 虚拟环境依赖，生成环境配置，并按选项构建前端。正式环境应从 `.env.example` 创建本地配置文件，不要将真实密钥提交到仓库。

前端也可单独安装依赖：

```bash
cd app/frontend
pnpm install
```

如项目环境使用 npm，可执行：

```bash
npm install
```

## 配置与安全

- RAG 配置参考 `aiops-rag-system/.env.example`。
- 控制台后端配置参考 `app/backend/.env.example`。
- 前端配置参考 `app/frontend/.env.example`。
- 生产环境必须设置随机、强度足够的 `JWT_SECRET_KEY` 和 `MASK_KEY`，不可依赖代码内置默认值。
- 正式环境关闭演示登录：

```bash
ENABLE_DEMO_LOGIN=false
```

- 演示账号、角色映射和中间件默认凭证见 `docs/DEMO_ACCOUNTS.md`。
- 发布前按照 `docs/OPEN_SOURCE_RELEASE_CHECKLIST.md` 清理 Git 跟踪文件并执行敏感信息扫描。

## 前端检查与构建

```bash
cd app/frontend
pnpm run lint
pnpm run build
pnpm run dev
```

## RAG 测试与静态检查

```bash
cd aiops-rag-system
pytest
python -m compileall src
```

完整回归测试包含 API、健康探针、依赖降级及 P99 优化场景。

## RAG 服务运行

在 `aiops-rag-system` 目录准备环境变量和依赖后运行：

```bash
uvicorn src.main:app
```

## 控制台后端运行

按照 `app/backend/README.md` 准备后端依赖、数据库和环境变量后启动服务。

## CI 验证

GitHub Actions 会自动执行：

1. RAG pytest；
2. 控制台后端 Python 静态编译检查；
3. 前端 ESLint 和生产构建。

## P99 压测验证

1. 按 `aiops-rag-system/README.md` 配置真实 Milvus、Embedding 和 LLM 依赖。
2. 对诊断接口施加递增并发压力。
3. 通过 `/metrics` 采集 Embedding、Milvus 检索、重排和 LLM 阶段 P99。
4. 调整超时、缓存、熔断、重试和检索参数后复测。

## 文档与部署

- 安装说明：`INSTALL.md`
- 开源发布：`docs/OPEN_SOURCE_RELEASE_CHECKLIST.md`
- 安全策略：`SECURITY.md`
- 贡献规范：`CONTRIBUTING.md`
- 用户使用：`docs/CONSOLE_USER_GUIDE.md`
- 常见问题：`docs/CONSOLE_FAQ.md`
- 部署方案：`docs/OPERATIONS_DEPLOYMENT_GUIDE.md`
- 值班手册：`docs/OPERATIONS_RUNBOOK.md`
- RAG 性能治理：`aiops-rag-system/README.md`
