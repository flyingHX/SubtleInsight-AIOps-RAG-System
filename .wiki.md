# Project Summary
项目提供面向企业知识运营与运维管理的控制台，帮助企业维护知识库、案例库及知识变更流程，并通过审批、去重合并、用户角色、审计日志和系统配置保障知识资产质量与业务数据安全。当前审批中心已支持完整展示知识合并、规则升级、新建案例及案例更新的业务内容。

# Project Module Description
- **知识库**：浏览和管理知识条目，支持标签筛选、案例库、变更记录及分页数据展示。
- **去重合并**：扫描相似案例、查看合并提案，并在审批内容中展示主案例和被合并案例的完整字段。
- **案例库**：展示和管理案例记录，支持完整案例字段、自动编号及关联日志实例。
- **审批中心**：按“待我审批”“我发起的”“我已处理”查看审批事项。
  - 合并审批展示合并策略、原因及双方完整案例内容。
  - 规则升级审批展示未知模板、升级规则条目预览及关联日志证据。
  - 新建案例审批展示完整案例字段、缺失字段标记和关联日志。
  - 案例更新审批展示变更前后快照及字段级 Diff。
- **变更记录**：查看知识库变更单及操作历史。
- **用户与角色**：管理用户、角色及访问入口，基于 RBAC 控制操作范围。
- **审计日志**：供运维人员查看系统操作记录及审批关联证据。
- **配置中心**：仅系统管理员及以上角色可访问和修改 LLM、Embedding、诊断审批策略及系统配置。
- **权限门控**：前端隐藏无权入口并阻止无权限请求，后端通过认证依赖执行最终校验。
- **错误与空数据处理**：权限不足时显示友好提示；分页接口、缺失字段和无匹配日志时安全降级展示。

# Directory Tree
```text
app/
├── backend/
│   └── services/
│       └── console_kb.py             # 知识库、案例及审批内容接口与序列化逻辑
└── frontend/
    └── src/
        ├── lib/
        │   └── console-api.ts        # 控制台 API 类型与调用封装
        └── pages/
            └── console/
                ├── ApprovalsPage.tsx # 审批中心列表及完整内容展示
                └── KbPage.tsx        # 知识库、案例库、变更记录及去重合并页面

verify_approval_content_tmp.py        # 审批内容接口端到端验收脚本
```

# File Description Inventory
| 文件 | 说明 |
|---|---|
| `app/backend/services/console_kb.py` | 扩展审批内容序列化；返回合并双方完整案例、规则升级预览、关联日志、新建案例完整字段、缺失字段及更新前后 Diff。 |
| `app/frontend/src/lib/console-api.ts` | 更新审批内容及 `MergeProposal` 等前端类型，适配新增完整内容结构。 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 渲染审批中心及审批详情，支持完整案例字段、日志实例、规则预览和字段级变更对比。 |
| `app/frontend/src/pages/console/KbPage.tsx` | 维护知识库相关页面、分页数据解析、案例及合并提案展示。 |
| `verify_approval_content_tmp.py` | 验证审批内容接口的合并、新建、升级及更新展示数据，并清理测试数据。 |

# Technology Stack
- **前端**：React、TypeScript
- **后端**：Python、知识库及审批服务接口
- **数据请求**：控制台 API 封装、分页响应模型、审批内容序列化
- **权限模型**：前后端 RBAC、认证依赖校验
- **构建与检查**：npm、Lint、生产构建、端到端验收脚本
- **核心数据能力**：案例完整字段、关联日志、规则预览、字段级 Diff、缺失字段标记

# Usage
## 安装前端依赖
```bash
cd app/frontend
npm install
```

## 代码检查
```bash
npm run lint
```

## 生产构建
```bash
npm run build
```

## 启动开发环境
```bash
npm run dev
```

## 审批内容验收
在后端服务和测试数据库准备完成后运行：
```bash
python verify_approval_content_tmp.py
```

验收重点：
- 合并审批展示双方完整案例字段及合并策略。
- 规则升级审批展示规则预览和关联日志实例。
- 新建案例审批展示全部案例字段、缺失字段和日志。
- 更新审批展示 before/after 快照及字段级 Diff。
- 未认证及无权限请求被拒绝。
- 无匹配日志时返回空列表，测试数据在验收后清理。
- 前端 lint 和生产构建通过。
