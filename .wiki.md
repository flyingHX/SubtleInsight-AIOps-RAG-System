# Project Summary

AIOps RAG System 是面向运维团队的智能告警诊断与知识运营平台。系统接收监控和日志告警，完成标准化、去重、存储、知识检索和大模型诊断，并通过 Web 控制台提供事件处理、知识库治理、规则管理、审批流和运行运维能力。

当前项目由异步诊断服务、后端管理 API 和前端控制台组成：

- 后端提供认证、演示登录、事件、知识库、规则、审批、审计、配置、存储和 AI 能力接口。
- 前端提供登录认证、角色化控制台、事件处理、知识库维护、规则管理、审批管理和运营页面。
- Kafka、Redis、Elasticsearch、Milvus 和 RAG 流水线承担告警处理与智能诊断。
- 关系数据库和 Alembic 提供业务数据持久化、权限控制、审计追踪和配置管理。
- 支持本地 Python 后端、Vite 前端、Docker Compose 基础设施以及 Lambda 处理入口。

# Project Module Description

## 告警接入与智能诊断

- Webhook 接收原始告警并进行模板提取、变量归一化、规则分类和事件标准化。
- Kafka 分发标准化事件，异步执行去重、RAG 诊断和存储。
- Redis 执行指纹去重与时间窗口抑制。
- Milvus 执行向量召回，并结合服务、故障类型、时间分区、拓扑相似度和人工反馈重排。
- LangChain 组织历史案例和提示词，调用兼容 OpenAI 协议的 LLM。
- 支持超时、无召回和解析失败降级。

## 运维控制台

- **Dashboard**：展示系统运行指标和运营概况。
- **告警工作台**：查看告警、事件详情、原始日志、拓扑信息并触发 AI 诊断。
- **知识库**：管理案例、版本、变更集、合并提案和人工反馈。
- **审批中心**：处理知识库、规则、配置和未知告警模板升级审批。
- **规则管理**：编辑、校验、发布、回滚 YAML 规则及处理未知告警模板。
- **运维设置**：管理系统配置、存储能力和审计信息。
- **博客与帮助内容**：展示产品说明和 Markdown 内容。

## 演示登录与权限

- 提供 `/api/v1/auth/demo-login` 演示登录端点。
- 演示登录复用既有 JWT 签发流程，并限制为预置演示账号。
- 可通过 `ENABLE_DEMO_LOGIN=false` 关闭演示登录。
- 前端登录页提供四个角色账号的快捷登录入口。
- 四个演示角色分别对应 `operator`、`sre`、`approver` 和 `sys_admin`。
- 受保护路由和后端权限依赖共同校验角色权限。
- 禁止审批发起人处理自己提交的审批单。

## 知识库与未知告警治理

- 保存故障案例、根因、解决方案、告警模板、拓扑快照和向量信息。
- 支持知识案例版本化、变更集、合并提案和审批发布。
- 支持未知告警模板收集、规则升级和状态流转。
- 通过审批后生成并启用新规则版本。
- 支持人工反馈评分并参与诊断结果重排。

## 审批与审计

- 支持知识库合并、规则升级和配置变更审批。
- 审批记录包含申请人、风险、处理人、时间线和最终状态。
- 关键操作写入审计日志，包括未知模板升级、规则重载和审批处理。
- 支持审批发起人隔离和角色化审批闭环。

## API 与数据服务

- 路由层负责 HTTP 接口和参数校验。
- 服务层封装认证、事件、知识库、审批、规则、存储和控制台业务。
- 模型层定义数据库实体。
- Schema 层定义认证、AI 能力和对象存储数据结构。
- Alembic 管理数据库迁移。
- Lambda Handler 支持无服务器入口。
- 健康检查接口用于运行状态探测。
- `fix_sequences.py` 用于修复显式种子 ID 导致的 PostgreSQL 序列不同步问题。

## 配置与可观测性

- `config/rules.yaml` 管理告警分类规则。
- `config/drain_patterns.yaml` 管理日志变量归一化。
- `.env` 管理数据库、消息系统、向量库、Embedding、LLM 和认证配置。
- Prometheus 指标覆盖标准化、去重和 RAG 请求。
- HTTP、数据库、请求摘要和耗时遥测模块提供运行诊断。
- 控制台配置支持数据库持久化和服务端管理。

# Directory Tree

```text
aiops-rag-system/
├── app/
│   ├── backend/
│   │   ├── alembic/                    # 数据库迁移配置和脚本
│   │   ├── core/                       # 配置、认证、数据库、加密和遥测
│   │   ├── data_models/                # JSON 数据模型
│   │   ├── dependencies/               # API 认证和数据库依赖
│   │   ├── models/                     # SQLAlchemy 业务实体
│   │   ├── routers/                    # 后端 HTTP 路由
│   │   ├── schemas/                    # API 请求和响应结构
│   │   ├── services/                   # 业务服务层
│   │   ├── scripts/                    # 数据初始化和数据库维护脚本
│   │   ├── skills_docs/                # AI、对象存储和 SDK 文档
│   │   ├── utils/                      # 后端通用工具
│   │   ├── main.py                     # 后端应用入口
│   │   ├── lambda_handler.py           # Lambda 入口
│   │   ├── diag_routes.py              # 诊断路由
│   │   ├── diag_routes2.py             # 扩展诊断路由
│   │   ├── requirements.txt            # 后端依赖
│   │   └── README.md                   # 后端运行说明
│   └── frontend/
│       ├── public/                     # 静态资源
│       ├── prerender/                  # 博客预渲染和站点地图生成
│       ├── src/
│       │   ├── components/             # 通用、博客和控制台组件
│       │   ├── contexts/               # 登录认证上下文
│       │   ├── hooks/                  # 前端 Hooks
│       │   ├── lib/                    # API、认证、配置和工具封装
│       │   ├── pages/                  # 首页、认证、博客和控制台页面
│       │   ├── api/                    # 前端业务 API
│       │   ├── App.tsx                 # 前端路由和根组件
│       │   └── main.tsx                # 前端启动入口
│       ├── package.json                # 前端依赖和脚本
│       ├── vite.config.ts              # Vite 配置
│       ├── tailwind.config.ts          # Tailwind 配置
│       └── README.md                   # 前端运行说明
├── config/
│   ├── drain_patterns.yaml             # Drain 变量归一化规则
│   ├── logging.yaml                   # 日志配置
│   └── rules.yaml                     # 告警分类规则
├── scripts/
│   ├── cleanup_cases.py               # 清理知识案例
│   ├── init_milvus.py                 # 初始化 Milvus
│   └── seed_cases.py                  # 导入初始案例
├── src/
│   ├── api/                            # 告警、诊断和反馈接口
│   ├── kafka_consumers/                # 去重、RAG 和存储消费者
│   ├── models/                         # 事件、案例和响应模型
│   ├── rag_pipeline/                   # Embedding、召回、重排和 LLM 流程
│   ├── standardization/                # 告警标准化引擎
│   ├── storage/                        # Redis 和 Elasticsearch 封装
│   ├── utils/                          # 日志和指标工具
│   ├── main.py                         # 原有 FastAPI 应用入口
│   └── runtime.py                      # 原有运行时组件管理
├── tests/                              # API、消费者、RAG、标准化和存储测试
├── docker-compose.yml                  # 本地基础设施编排
├── requirements.txt                    # 原有 Python 依赖
├── pytest.ini                          # pytest 配置
├── app/start_app_v2.sh                 # 新版应用启动脚本
├── verify_console_api_tmp.py           # 控制台 API 验证脚本
├── uploads/                            # 上传文件和页面资源
└── .env.example                        # 环境变量模板
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `app/backend/routers/auth.py` | 登录、回调、退出和演示登录接口 |
| `app/backend/schemas/auth.py` | 认证请求和响应结构 |
| `app/backend/core/` | 配置、认证、数据库、枚举、加密和敏感信息处理 |
| `app/backend/core/telemetry/` | HTTP、SQLAlchemy、请求摘要和耗时遥测 |
| `app/backend/alembic/` | 数据库迁移环境和版本脚本 |
| `app/backend/models/` | 事件、知识库、审批、审计、规则和配置实体 |
| `app/backend/routers/` | 认证、事件、知识库、审批、规则、设置和存储接口 |
| `app/backend/services/` | 控制台、AI、知识库、审批、用户和存储业务 |
| `app/backend/schemas/` | AI 能力和对象存储 API 数据契约 |
| `app/backend/scripts/fix_sequences.py` | 将业务表 PostgreSQL 序列同步到 `MAX(id)+1` |
| `app/backend/scripts/seed_console_demo.py` | 初始化控制台演示数据 |
| `app/backend/main.py` | 初始化新版后端应用和路由 |
| `app/backend/lambda_handler.py` | Lambda 部署入口 |
| `app/frontend/src/pages/console/` | 事件、知识库、审批、规则和运维页面 |
| `app/frontend/src/components/console/ConsoleLayout.tsx` | 控制台布局、登录屏和演示账号快捷入口 |
| `app/frontend/src/components/ui/` | 通用 UI 组件 |
| `app/frontend/src/lib/` | API、认证、配置、博客和控制台请求封装 |
| `app/frontend/src/contexts/` | 管理前端认证状态 |
| `app/frontend/prerender/` | 生成博客页面和站点地图 |
| `app/frontend/package.json` | 前端依赖、构建和开发命令 |
| `src/standardization/` | 将原始告警转换为标准化事件 |
| `src/kafka_consumers/` | 异步执行去重、诊断和冷存储 |
| `src/rag_pipeline/` | 向量化、召回、重排、Prompt 和 LLM 推理 |
| `src/storage/` | Redis 与 Elasticsearch 封装 |
| `src/api/` | 原有告警接入、人工诊断和反馈接口 |
| `config/` | 标准化、日志和规则配置 |
| `scripts/` | 向量库和知识案例维护工具 |
| `tests/` | 核心模块自动化测试 |
| `app/start_app_v2.sh` | 启动新版前后端应用 |
| `verify_console_api_tmp.py` | 验证控制台后端 API |
| `uploads/` | 保存上传的图片等资源 |

# Technology Stack

- **后端语言与运行时**：Python 3.10+
- **后端框架**：FastAPI、Uvicorn
- **前端框架**：React、TypeScript、Vite
- **样式与 UI**：Tailwind CSS、Radix 风格组件、PostCSS
- **数据校验与 ORM**：Pydantic、SQLAlchemy
- **数据库迁移**：Alembic
- **数据库**：PostgreSQL
- **消息系统**：Apache Kafka
- **缓存与去重**：Redis
- **向量数据库**：Milvus
- **冷存储与审计**：Elasticsearch
- **RAG 编排**：LangChain
- **向量模型**：BGE-M3 或兼容 Embedding 服务
- **大语言模型**：兼容 OpenAI 接口的 DeepSeek、GPT、Qwen 等服务
- **认证与安全**：JWT、角色权限、受保护路由、敏感信息掩码与加密
- **可观测性**：Prometheus Client、自定义 HTTP 和数据库遥测
- **测试与检查**：pytest、Python compileall、ESLint
- **部署方式**：Docker Compose、Python 进程、前端构建和 Lambda Handler

# Usage

## 安装后端依赖

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Windows 环境：

```bash
.venv\Scripts\activate
```

## 安装前端依赖

```bash
cd app/frontend
npm install
```

## 配置环境变量

准备 `.env`，参考 `.env.example` 配置数据库、认证、Kafka、Redis、Elasticsearch、Milvus、Embedding、LLM 和对象存储信息。

如需启用演示登录，设置：

```env
ENABLE_DEMO_LOGIN=true
```

生产环境建议设置为 `false`。

## 启动基础设施

```bash
docker compose up -d
```

## 执行数据库迁移

```bash
cd app/backend
alembic upgrade head
```

## 修复数据库序列

当使用显式 ID 初始化种子数据后，执行：

```bash
cd app/backend
python scripts/fix_sequences.py
```

该脚本会根据各业务表的最大 ID 同步 PostgreSQL 序列，避免新增审批单等记录发生主键冲突。

## 初始化数据

```bash
python scripts/init_milvus.py
python scripts/seed_cases.py
```

初始化控制台演示数据：

```bash
cd app/backend
python scripts/seed_console_demo.py
```

## 启动后端

新版后端：

```bash
cd app/backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

原有诊断服务：

```bash
python -m src.main
```

统一启动：

```bash
bash app/start_app_v2.sh
```

## 启动前端

```bash
cd app/frontend
npm run dev
```

生产构建和预览：

```bash
npm run build
npm run preview
```

## 清理知识案例

```bash
python scripts/cleanup_cases.py
```

执行前确认目标集合、时间范围和清理策略。

## 运行测试与静态检查

```bash
pytest
python -m compileall src tests scripts app/backend
cd app/frontend
npm run lint
```
