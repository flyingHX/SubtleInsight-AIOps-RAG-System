# Project Summary

AIOps RAG System 是面向运维团队的智能告警诊断与知识运营平台。系统接收监控与日志告警，完成标准化、去重、存储、知识检索和大模型诊断，并通过 Web 控制台支持事件处理、知识库治理、规则管理、审批流和运行运维。

项目由异步诊断服务、后端管理 API 和前端控制台组成：

- 后端提供认证、事件、知识库、规则、审批、审计、配置、存储和 AI 能力。
- 前端提供登录认证、角色化控制台、事件处理、知识库维护、审批管理、规则管理和帮助中心。
- Kafka、Redis、Elasticsearch、Milvus 与 RAG 流水线承担告警处理和智能诊断。
- PostgreSQL 与 Alembic 提供业务持久化、权限控制、审计追踪和配置管理。
- 支持本地 Python 后端、Vite 前端、Docker Compose 基础设施及 Lambda 入口。
- 审批中心可展示审批业务内容，并对知识库修改提供变更前后及字段级差异对比。
- 新建知识库案例自动生成 `KB-YYYYMMDD-NNN` 格式的案例 ID。
- 控制台新增使用手册、FAQ、部署指南和运维 Runbook。

# Project Module Description

## 告警接入与智能诊断

- Webhook 接收原始告警，执行模板提取、变量归一化、规则分类和事件标准化。
- Kafka 分发标准化事件，异步执行去重、RAG 诊断和存储。
- Redis 执行指纹去重与时间窗口抑制。
- Milvus 执行向量召回，并结合服务、故障类型、时间分区、拓扑相似度和人工反馈重排。
- LangChain 组织历史案例和提示词，调用兼容 OpenAI 协议的 LLM。
- 支持超时、无召回和解析失败降级。

## 运维控制台

- **Dashboard**：展示系统运行指标和运营概况。
- **告警工作台**：查看告警、事件详情、原始日志、拓扑信息并触发 AI 诊断。
- **知识库**：管理案例、版本、变更集、合并提案和人工反馈。
- **审批中心**：查看审批类型、申请内容、风险信息以及变更前后对比。
- **规则管理**：编辑、校验、发布、回滚 YAML 规则并处理未知告警模板。
- **运维设置**：管理系统配置、存储能力和审计信息。
- **帮助中心**：通过 `/help` 提供使用手册、FAQ 和运维速览。
- **博客与帮助内容**：展示产品说明和 Markdown 内容。

## 审批内容与变更对比

- `GET /api/v1/console/approvals/{id}/content` 返回审批业务内容。
- `kb_edit` 审批返回 `before`、`after` 和字段级 `diff`。
- `merge` 审批返回合并提案及合并策略。
- `rule_promote` 审批返回告警模板与建议分类。
- 审批人可在审批卡片中展开内容和对比详情。
- 禁止审批发起人处理自己提交的审批单。

## 知识库案例管理

- 保存故障案例、根因、解决方案、告警模板、拓扑快照和向量信息。
- 支持案例版本化、变更集、合并提案和审批发布。
- 新建案例不再要求手工输入 ID，由后端按日期和递增序号生成 `KB-YYYYMMDD-NNN`。
- 生成过程支持并发场景下的在途审批去重。
- 支持未知告警模板收集、规则升级和状态流转。
- 支持人工反馈评分并参与诊断结果重排。

## 演示登录与权限

- 提供 `/api/v1/auth/demo-login` 演示登录端点。
- 演示登录复用 JWT 签发流程，并限制为预置演示账号。
- 可通过 `ENABLE_DEMO_LOGIN=false` 关闭演示登录。
- 前端支持四个演示角色快捷登录：`operator`、`sre`、`approver` 和 `sys_admin`。
- 受保护路由和后端权限依赖共同校验角色权限。

## API 与数据服务

- 路由层负责 HTTP 接口和参数校验。
- 服务层封装认证、事件、知识库、审批、规则、存储和控制台业务。
- 模型层定义数据库实体。
- Schema 层定义 API、AI 能力和对象存储数据结构。
- Alembic 管理数据库迁移。
- Lambda Handler 支持无服务器入口。
- `/health` 用于服务健康检查。
- `fix_sequences.py` 用于修复显式种子 ID 导致的 PostgreSQL 序列不同步问题。

## 配置、可观测性与运维

- `config/rules.yaml` 管理告警分类规则。
- `config/drain_patterns.yaml` 管理日志变量归一化。
- `.env` 管理数据库、消息系统、向量库、Embedding、LLM 和认证配置。
- Prometheus 指标覆盖标准化、去重和 RAG 请求。
- HTTP、数据库、请求摘要和耗时遥测模块提供运行诊断。
- 部署指南覆盖环境变量、数据库、代理、升级和部署方案。
- 运维 Runbook 覆盖值班、健康检查、日志、备份恢复和故障处理。

# Directory Tree

```text
aiops-rag-system/
├── app/
│   ├── backend/
│   │   ├── alembic/                    # 数据库迁移配置和脚本
│   │   ├── core/                       # 配置、认证、数据库、加密和遥测
│   │   ├── data_models/                # JSON 数据模型
│   │   ├── dependencies/               # API 认证和数据库依赖
│   │   ├── models/                     # SQLAlchemy 业务实体
│   │   ├── routers/                    # 后端 HTTP 路由
│   │   ├── schemas/                    # API 请求和响应结构
│   │   ├── services/                   # 业务服务层
│   │   ├── scripts/                    # 数据初始化和数据库维护脚本
│   │   ├── skills_docs/                # AI、对象存储和 SDK 文档
│   │   ├── utils/                      # 后端通用工具
│   │   ├── main.py                     # 后端应用入口
│   │   ├── lambda_handler.py           # Lambda 入口
│   │   ├── diag_routes.py              # 诊断路由
│   │   ├── diag_routes2.py             # 扩展诊断路由
│   │   ├── requirements.txt            # 后端依赖
│   │   └── README.md                   # 后端运行说明
│   ├── docs/
│   │   ├── CONSOLE_USER_GUIDE.md       # 控制台使用手册
│   │   ├── CONSOLE_FAQ.md              # 控制台常见问题
│   │   ├── OPERATIONS_DEPLOYMENT_GUIDE.md # 部署方案与升级指南
│   │   └── OPERATIONS_RUNBOOK.md       # 值班、监控和故障处理手册
│   └── frontend/
│       ├── public/                     # 静态资源
│       ├── prerender/                  # 博客预渲染和站点地图生成
│       ├── src/
│       │   ├── components/             # 通用、博客和控制台组件
│       │   ├── contexts/               # 登录认证上下文
│       │   ├── hooks/                  # 前端 Hooks
│       │   ├── lib/                    # API、认证、配置和工具封装
│       │   ├── pages/                  # 首页、认证、帮助、博客和控制台页面
│       │   ├── api/                    # 前端业务 API
│       │   ├── App.tsx                 # 前端路由和根组件
│       │   └── main.tsx                # 前端启动入口
│       ├── package.json                # 前端依赖和脚本
│       ├── vite.config.ts              # Vite 配置
│       ├── tailwind.config.ts          # Tailwind 配置
│       └── README.md                   # 前端运行说明
├── config/
│   ├── drain_patterns.yaml             # Drain 变量归一化规则
│   ├── logging.yaml                    # 日志配置
│   └── rules.yaml                      # 告警分类规则
├── scripts/
│   ├── cleanup_cases.py                # 清理知识案例
│   ├── init_milvus.py                  # 初始化 Milvus
│   └── seed_cases.py                   # 导入初始案例
├── src/
│   ├── api/                            # 告警、诊断和反馈接口
│   ├── kafka_consumers/                # 去重、RAG 和存储消费者
│   ├── models/                         # 事件、案例和响应模型
│   ├── rag_pipeline/                   # Embedding、召回、重排和 LLM 流程
│   ├── standardization/                # 告警标准化引擎
│   ├── storage/                        # Redis 和 Elasticsearch 封装
│   ├── utils/                          # 日志和指标工具
│   ├── main.py                         # 原有 FastAPI 应用入口
│   └── runtime.py                      # 原有运行时组件管理
├── tests/                              # API、消费者、RAG、标准化和存储测试
├── docker-compose.yml                  # 本地基础设施编排
├── requirements.txt                    # 原有 Python 依赖
├── pytest.ini                          # pytest 配置
├── app/start_app_v2.sh                # 新版应用启动脚本
├── verify_console_api_tmp.py           # 控制台 API 验证脚本
├── uploads/                            # 上传文件和页面资源
└── .env.example                        # 环境变量模板
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `app/backend/routers/console.py` | 控制台事件、知识库、审批及审批内容接口 |
| `app/backend/services/console_kb.py` | 知识库案例、变更集和自动编号业务 |
| `app/backend/routers/auth.py` | 登录、回调、退出和演示登录接口 |
| `app/backend/core/` | 配置、认证、数据库、枚举、加密和敏感信息处理 |
| `app/backend/core/telemetry/` | HTTP、SQLAlchemy、请求摘要和耗时遥测 |
| `app/backend/alembic/` | 数据库迁移环境和版本脚本 |
| `app/backend/models/` | 事件、知识库、审批、审计、规则和配置实体 |
| `app/backend/routers/` | 后端 HTTP 路由集合 |
| `app/backend/services/` | 控制台、AI、知识库、审批、用户和存储业务 |
| `app/backend/scripts/fix_sequences.py` | 将业务表 PostgreSQL 序列同步到 `MAX(id)+1` |
| `app/backend/scripts/seed_console_demo.py` | 初始化控制台演示数据 |
| `app/backend/main.py` | 初始化新版后端应用和路由 |
| `app/backend/lambda_handler.py` | Lambda 部署入口 |
| `app/frontend/src/pages/console/ApprovalsPage.tsx` | 审批列表、审批内容和变更对比展示 |
| `app/frontend/src/pages/console/KbPage.tsx` | 知识库案例管理和自动编号新建流程 |
| `app/frontend/src/pages/HelpPage.tsx` | 使用手册、FAQ 和运维速览页面 |
| `app/frontend/src/App.tsx` | 前端路由，包括 `/help` |
| `app/frontend/src/components/console/ConsoleLayout.tsx` | 控制台布局、权限导航和帮助入口 |
| `app/frontend/src/lib/console-api.ts` | 控制台 API 请求封装 |
| `app/frontend/src/components/console/shared/` | 控制台共享展示组件，包括差异表格 |
| `app/frontend/src/pages/console/` | 事件、知识库、审批、规则和运维页面 |
| `app/frontend/src/lib/` | API、认证、配置、博客和控制台请求封装 |
| `app/frontend/prerender/` | 生成博客页面和站点地图 |
| `src/standardization/` | 将原始告警转换为标准化事件 |
| `src/kafka_consumers/` | 异步执行去重、诊断和冷存储 |
| `src/rag_pipeline/` | 向量化、召回、重排、Prompt 和 LLM 推理 |
| `src/storage/` | Redis 与 Elasticsearch 封装 |
| `src/api/` | 原有告警接入、人工诊断和反馈接口 |
| `config/` | 标准化、日志和规则配置 |
| `scripts/` | 向量库和知识案例维护工具 |
| `tests/` | 核心模块自动化测试 |
| `app/docs/CONSOLE_USER_GUIDE.md` | 控制台完整使用流程 |
| `app/docs/CONSOLE_FAQ.md` | 控制台常见问题与处理建议 |
| `app/docs/OPERATIONS_DEPLOYMENT_GUIDE.md` | 部署、环境变量、数据库、代理和升级方案 |
| `app/docs/OPERATIONS_RUNBOOK.md` | 值班、健康检查、日志、备份恢复和故障处理 |
| `app/start_app_v2.sh` | 启动新版前后端应用 |
| `verify_console_api_tmp.py` | 验证控制台后端 API |
| `uploads/` | 保存上传的图片等资源 |

# Technology Stack

- **后端语言与运行时**：Python 3.10+
- **后端框架**：FastAPI、Uvicorn
- **前端框架**：React、TypeScript、Vite
- **样式与 UI**：Tailwind CSS、Radix 风格组件、PostCSS
- **Markdown 渲染**：前端 Markdown/Prose 内容渲染组件
- **数据校验与 ORM**：Pydantic、SQLAlchemy
- **数据库迁移**：Alembic
- **数据库**：PostgreSQL
- **消息系统**：Apache Kafka
- **缓存与去重**：Redis
- **向量数据库**：Milvus
- **冷存储与审计**：Elasticsearch
- **RAG 编排**：LangChain
- **向量模型**：BGE-M3 或兼容 Embedding 服务
- **大语言模型**：兼容 OpenAI 接口的 DeepSeek、GPT、Qwen 等服务
- **认证与安全**：JWT、角色权限、受保护路由、敏感信息掩码与加密
- **可观测性**：Prometheus Client、自定义 HTTP 和数据库遥测
- **测试与检查**：pytest、Python compileall、ESLint、前端 lint 和 build
- **部署方式**：Docker Compose、Python 进程、前端构建和 Lambda Handler

# Usage

## 安装后端依赖

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Windows 环境：

```bash
.venv\Scripts\activate
```

## 安装前端依赖

```bash
cd app/frontend
npm install
```

也可使用项目已配置的 pnpm 工作流安装依赖：

```bash
pnpm install
```

## 配置环境变量

准备 `.env`，参考 `.env.example` 配置数据库、认证、Kafka、Redis、Elasticsearch、Milvus、Embedding、LLM 和对象存储信息。

如需启用演示登录：

```env
ENABLE_DEMO_LOGIN=true
```

生产环境建议设置为 `false`。

## 启动基础设施

```bash
docker compose up -d
```

## 执行数据库迁移

```bash
cd app/backend
alembic upgrade head
```

## 初始化数据

```bash
python scripts/init_milvus.py
python scripts/seed_cases.py
```

初始化控制台演示数据：

```bash
cd app/backend
python scripts/seed_console_demo.py
```

## 修复数据库序列

使用显式 ID 初始化种子数据后执行：

```bash
cd app/backend
python scripts/fix_sequences.py
```

脚本会根据各业务表最大 ID 同步 PostgreSQL 序列，避免新增记录发生主键冲突。

## 启动后端

新版后端：

```bash
cd app/backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

原有诊断服务：

```bash
python -m src.main
```

统一启动：

```bash
bash app/start_app_v2.sh
```

## 启动前端

```bash
cd app/frontend
npm run dev
```

## 生产构建和预览

```bash
npm run build
npm run preview
```

使用 pnpm 时：

```bash
pnpm lint
pnpm build
```

## 清理知识案例

```bash
python scripts/cleanup_cases.py
```

执行前确认目标集合、时间范围和清理策略。

## 运行测试与静态检查

```bash
pytest
python -m compileall src tests scripts app/backend
cd app/frontend
npm run lint
npm run build
```

## 运维与部署

部署前参考：

- `app/docs/OPERATIONS_DEPLOYMENT_GUIDE.md`
- `app/docs/OPERATIONS_RUNBOOK.md`

控制台用户可从帮助中心查看：

- `app/docs/CONSOLE_USER_GUIDE.md`
- `app/docs/CONSOLE_FAQ.md`
