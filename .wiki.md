# Project Summary
项目提供面向企业运营与系统管理的控制台，支持用户与角色管理、审计日志及系统配置。通过前后端 RBAC 控制不同角色的操作范围，保障运维配置与业务数据安全。

# Project Module Description
- **用户与角色**：管理用户、角色及访问入口，仅授权角色可用。
- **审计日志**：供运维人员查看系统操作记录。
- **配置中心**：仅系统管理员及以上角色可访问和修改 LLM、Embedding、诊断审批策略及系统配置。
- **权限门控**：前端根据角色隐藏无权标签和菜单，并阻止无权限请求；后端继续执行最终权限校验。
- **错误提示**：权限不足时展示友好提示，避免直接暴露 403 错误或空白页面。

# Directory Tree
```text
app/
└── frontend/
    └── src/
        └── pages/
            └── console/
                └── OpsPage.tsx    # 审计与配置页面，按角色控制标签展示和配置请求
```

# File Description Inventory
| 文件 | 说明 |
|---|---|
| `app/frontend/src/pages/console/OpsPage.tsx` | 运维控制台页面；为配置中心增加角色门控，非管理员不发起配置请求并隐藏无权限入口。 |

# Technology Stack
- 前端：React、TypeScript
- 构建与检查：前端项目既有 npm 脚本、Lint、生产构建
- 权限模型：前后端 RBAC
- 验证范围：API 权限校验、浏览器角色视图、前端构建与回归测试

# Usage
## 安装依赖
```bash
cd app/frontend
npm install
```

## 代码检查
```bash
npm run lint
```

## 生产构建
```bash
npm run build
```

## 启动开发环境
```bash
npm run dev
```

## 权限验证
- 使用非管理员演示账号进入运维页面，确认仅显示审计日志，不发起配置请求。
- 使用系统管理员账号进入运维页面，确认配置中心标签及配置数据正常加载。
- 验证用户与角色入口按角色正确显示或隐藏。
