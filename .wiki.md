# Project Summary

AIOps RAG System 是面向运维告警处理的智能诊断平台，负责接收来自监控与日志系统的原始告警，将非结构化信息标准化、去重聚合，并通过知识库检索与 LLM 推理生成根因分析和处置建议。系统支持历史案例沉淀、人工反馈、自诊断触发和知识库持续优化，帮助降低告警噪声并缩短故障定位时间。

当前项目已完成核心开发，包含标准化引擎、Kafka 事件分发、Redis 去重、Elasticsearch 冷存储、Milvus 向量检索、LangChain RAG 流水线、HTTP API、可观测性指标和测试套件。全量 30 个测试通过，源码编译检查通过。

# Project Module Description

## 告警接入与标准化

- `POST /api/v1/webhook` 接收原始告警。
- 使用 Drain 提取日志模板，并执行时间、线程、IP、UUID、Hash、数字等变量归一化。
- 通过 `config/rules.yaml` 进行关键词、正则、排除条件和插件规则匹配。
- 产出包含 `error_type`、置信度、模板、服务、集群和拓扑信息的标准化事件。
- 规则支持热加载，加载成功后自动清理分类缓存。
- 未达到置信度阈值的事件标记为未分类，并进入后续知识沉淀流程。

## Kafka 事件分发

标准化事件写入 Kafka 后，由三个独立消费组并行处理：

- 去重消费者：使用 Redis 指纹和时间窗口抑制重复告警。
- RAG 消费者：处理严重告警或人工触发的诊断请求。
- 存储消费者：批量写入 Elasticsearch，支持审计和离线分析。

## RAG 智能诊断

- 使用 Embedding 模型生成告警查询向量。
- 在 Milvus 中先进行服务、故障类型和时间分区过滤，再执行 ANN 向量召回。
- 使用拓扑相似度、时间衰减和人工反馈进行重排。
- 通过 LangChain Prompt 组装历史案例并调用 LLM。
- LLM 超时、无召回结果或输出解析失败时，自动降级为历史案例解决方案或人工介入建议。

## 知识库与反馈闭环

- Milvus 保存故障案例、根因、解决方案、告警模板、拓扑快照和向量。
- 支持人工反馈评分，反馈结果参与后续重排。
- 支持自动或人工解决案例写入知识库。
- 提供案例初始化、种子数据导入和历史案例清理脚本。

## HTTP API

- Webhook：接收和标准化告警。
- Diagnostic：按事件执行人工触发诊断。
- Feedback：更新知识案例有效性评分。

## 配置与可观测性

- `rules.yaml`：标准化分类规则。
- `drain_patterns.yaml`：日志变量替换规则。
- `logging.yaml`：日志输出配置。
- `.env.example`：运行环境变量模板。
- Prometheus 指标覆盖标准化请求、延迟、未知率、RAG 请求、RAG 延迟和去重率。

# Directory Tree

```text
aiops-rag-system/
├── .env.example                         # 环境变量模板
├── .gitignore                           # Git 忽略规则
├── README.md                            # 项目说明与运行指南
├── docker-compose.yml                   # 本地基础设施编排
├── pytest.ini                           # pytest 配置
├── requirements.txt                     # Python 依赖
├── config/
│   ├── drain_patterns.yaml              # Drain 变量归一化规则
│   ├── logging.yaml                     # 日志配置
│   └── rules.yaml                       # 告警分类规则
├── scripts/
│   ├── cleanup_cases.py                 # 知识案例清理
│   ├── init_milvus.py                   # Milvus 集合和索引初始化
│   └── seed_cases.py                    # 初始案例导入
├── src/
│   ├── __init__.py                      # 顶层包
│   ├── main.py                          # FastAPI 应用入口
│   ├── config.py                        # 配置加载
│   ├── kafka_producer.py                # Kafka 事件生产者
│   ├── runtime.py                      # 运行时组件管理
│   ├── api/
│   │   ├── __init__.py
│   │   ├── diagnostic.py                # 人工诊断接口
│   │   ├── feedback.py                  # 反馈接口
│   │   └── webhook.py                   # 告警 Webhook 接口
│   ├── kafka_consumers/
│   │   ├── __init__.py                  # 消费者启动入口
│   │   ├── dedup_consumer.py            # 去重聚合消费者
│   │   ├── rag_consumer.py              # RAG 诊断消费者
│   │   └── storage_consumer.py          # 存储消费者
│   ├── models/
│   │   ├── case.py                      # 知识案例模型
│   │   ├── event.py                     # 原始和标准化事件模型
│   │   └── response.py                  # API 响应模型
│   ├── rag_pipeline/
│   │   ├── __init__.py
│   │   ├── embedder.py                  # 文本向量化
│   │   ├── llm_client.py                # LLM 客户端
│   │   ├── milvus_client.py             # Milvus 访问封装
│   │   ├── pipeline.py                  # RAG 主流程
│   │   ├── prompt_builder.py             # LangChain Prompt 构建
│   │   └── reranker.py                  # 业务重排
│   ├── standardization/
│   │   ├── __init__.py
│   │   ├── drain_extractor.py            # Drain 模板提取
│   │   ├── engine.py                    # 标准化核心引擎
│   │   ├── plugin_manager.py            # 规则插件管理
│   │   └── rule_loader.py               # 规则加载与热更新
│   ├── storage/
│   │   ├── __init__.py
│   │   ├── es_client.py                 # Elasticsearch 封装
│   │   └── redis_client.py              # Redis 封装
│   └── utils/
│       ├── logger.py                    # 统一日志
│       └── metrics.py                   # Prometheus 指标
└── tests/
    ├── __init__.py
    ├── conftest.py                      # 测试夹具
    ├── test_api.py                      # API 测试
    ├── test_consumers.py                # Kafka 消费者测试
    ├── test_rag_pipeline.py             # RAG 流程测试
    ├── test_standardization.py          # 标准化测试
    └── test_storage.py                  # 存储模块测试
```

# File Description Inventory

| 文件或目录 | 说明 |
|---|---|
| `config/` | 管理告警分类、Drain 归一化和日志配置 |
| `scripts/` | 提供 Milvus 初始化、案例导入和案例清理运维工具 |
| `src/api/` | 对外提供告警接入、人工诊断和反馈能力 |
| `src/standardization/` | 将原始日志转换为可检索、可路由的标准化事件 |
| `src/kafka_consumers/` | 执行去重、RAG 诊断和冷存储三类异步任务 |
| `src/rag_pipeline/` | 完成向量召回、重排、Prompt 构建和 LLM 推理 |
| `src/storage/` | 封装 Redis 与 Elasticsearch 数据访问 |
| `src/models/` | 定义事件、知识案例和接口响应数据结构 |
| `src/utils/` | 提供日志与 Prometheus 指标能力 |
| `src/main.py` | 初始化应用、路由和后台消费者 |
| `src/runtime.py` | 管理应用运行时组件和生命周期 |
| `docker-compose.yml` | 编排本地 Kafka、Redis、Elasticsearch、Milvus 等依赖 |
| `requirements.txt` | 声明 Python 运行和测试依赖 |
| `pytest.ini` | 配置测试发现和执行参数 |
| `tests/` | 覆盖 API、消费者、RAG、标准化和存储模块 |

# Technology Stack

- **语言与运行时**：Python 3.10+
- **Web 框架**：FastAPI、Uvicorn
- **数据校验**：Pydantic
- **消息系统**：Apache Kafka
- **缓存与去重**：Redis
- **向量数据库**：Milvus
- **冷存储与审计**：Elasticsearch
- **RAG 编排**：LangChain
- **向量模型**：BGE-M3 或兼容 Embedding 服务
- **大语言模型**：兼容 OpenAI 接口的 DeepSeek、GPT、Qwen 等服务
- **配置格式**：YAML、环境变量
- **监控指标**：Prometheus Client
- **测试工具**：pytest
- **部署方式**：Docker Compose 与 Python 进程部署

# Usage

## 安装依赖

```bash
cd aiops-rag-system
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
```

Windows 环境可使用：

```bash
.venv\Scripts\activate
```

## 启动基础设施

```bash
docker compose up -d
```

根据 `.env` 配置 Kafka、Redis、Elasticsearch、Milvus、Embedding 和 LLM 服务连接信息。

## 初始化 Milvus

```bash
python scripts/init_milvus.py
python scripts/seed_cases.py
```

## 启动应用

```bash
python -m src.main
```

也可以使用 Uvicorn：

```bash
uvicorn src.main:app --host 0.0.0.0 --port 8080
```

## 清理知识案例

```bash
python scripts/cleanup_cases.py
```

执行前应确认目标集合、时间范围和清理策略配置正确。

## 运行测试

```bash
pytest
```

当前测试结果：30 个测试全部通过。

## 静态检查

```bash
python -m compileall src tests scripts
```
